module Sobel(clk, rst, gray_in, en, Gx, Gy, done);
//convolution kernel for applying sobel
// we need 9 adjacent values -> output 1 pixel value
input clk, rst, en;

input [11:0] gray_in;
///need to figure out where to implement


//would be easiest to implement from within memory since 